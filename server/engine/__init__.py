"""Phase 2 constraint engine — placeholder.

Planned modules (all pure functions, $0, unit-tested against the real 58-garment /
18-look closet):
  constraints.py  completeness rules + warmth/formality banding (v2 §5.B.2)
  colour.py       LAB conversion, neutral detection, harmony scoring (invariant #6)
  gaps.py         outfit enumeration, participation counting, orphan detection (v2 §5.D.4)

Do not build until Phase 1 backfill lands — the engine needs real garment attributes
to test against.
"""
