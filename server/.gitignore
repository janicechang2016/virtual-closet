.env
.app_secret
.railway_token
__pycache__/
*.pyc

# Generated by the Phase 1 scripts — reproducible, not source.
# colors.json IS committed: it is the measurement record and worth diffing.
scripts/backfill.sql
scripts/attrs.sql
scripts/occasions.sql
scripts/purchase.sql
scripts/attr_grid.html
scripts/occasion_form.html
scripts/purchase_form.html
scripts/color_review.html
# closet_snapshot.json is deliberately TRACKED (07-26): the Vercel build reads it
# to generate the /insights, /galaxy and /stylist payloads, and that build has no
# Postgres and no .app_secret. Untracked, the deploy simply cannot produce them.
# Refresh with server/scripts/dump_closet.py and commit the result.
scripts/ranking_review.html
