# Copy to .env (gitignored). Keys never leave this machine except in API calls.
FAL_KEY=            # required — fal.ai dashboard → API keys
GEMINI_API_KEY=     # optional — direct Nano Banana route (Google AI Studio)
FASHN_API_KEY=      # optional — try-on specialist benchmark arm
ANTHROPIC_API_KEY=  # optional — scripted auto-QA judge (QA can also run inside Claude Code at $0)
